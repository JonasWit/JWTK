class AccessKey {
    id: number;
    name: string;
    applicationType: string;

    constructor(id: number, name: string, applicationType: string) {
        this.id = id;
        this.name = name;
        this.applicationType = applicationType;
    }
    
    
    
} 